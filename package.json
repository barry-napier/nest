{
  "name": "nest",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "live-server --port=3000 --open=/public/index.html",
    "dev": "live-server --port=3000 --open=/public/index.html --watch=src",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "stylelint \"src/**/*.css\"",
    "lint:fix": "stylelint \"src/**/*.css\" --fix"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "live-server": "^1.2.2",
    "prettier": "^3.6.2",
    "stylelint": "^16.21.1",
    "stylelint-config-recommended": "^16.0.0"
  }
}
